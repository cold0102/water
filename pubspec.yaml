name: water_reminder
description: 极简喝水记录
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_local_notifications: ^16.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.5
  build_runner: ^2.4.6

flutter:
  uses-material-design: true